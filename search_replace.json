
[
  {
    "search": "   - **When NOT to use:**\n      - NEVER use this tool to verify whether a SearchReplaceTool edit was applied correctly — use GrepTool instead for targeted, token-efficient verification.",
    "replace": "   - **When NOT to use:** Do not use this tool to verify whether a SearchReplaceTool edit was applied correctly — use GrepTool instead for targeted, token-efficient verification. Also, do not use it to read files one at a time if multiple related file reads can be batched together.",
    "comment": "Surgically captures the multi-line sub-bullet structure in the raw source file and compresses it into the updated layout."
  }
]
